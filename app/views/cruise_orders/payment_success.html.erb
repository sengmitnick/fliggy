<div class="min-h-screen bg-surface flex items-center justify-center p-4">
  <div class="max-w-md w-full space-y-6">
    <!-- Success Icon -->
    <div class="text-center">
      <div class="inline-flex items-center justify-center w-20 h-20 rounded-full bg-green-100 mb-4">
        <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
        </svg>
      </div>
      <h1 class="text-2xl font-bold text-gray-900 mb-2">支付成功！</h1>
      <p class="text-gray-600">您的游轮订单已确认</p>
    </div>

    <!-- Order Info Card -->
    <div class="bg-white rounded-xl p-6 shadow-sm space-y-4">
      <div class="flex gap-4">
        <%= image_tag @cruise_ship.image_url, alt: @cruise_ship.name, class: "w-20 h-20 rounded-lg object-cover flex-shrink-0" %>
        <div class="flex-1 min-w-0">
          <h2 class="font-semibold text-gray-900 mb-1 truncate"><%= @cruise_ship.name %></h2>
          <p class="text-sm text-gray-600 mb-1">
            <%= @cruise_order.cruise_product.cruise_sailing.cruise_route.name %>航线
          </p>
          <p class="text-sm text-gray-500">
            <%= @cruise_order.cruise_product.cruise_sailing.date_range %>
          </p>
        </div>
      </div>

      <div class="pt-4 border-t border-gray-100 space-y-3">
        <div class="flex justify-between text-sm">
          <span class="text-gray-600">订单号</span>
          <span class="font-mono text-gray-900"><%= @cruise_order.order_number %></span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="text-gray-600">支付金额</span>
          <span class="font-bold text-green-600">¥<%= @cruise_order.total_price %></span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="text-gray-600">支付时间</span>
          <span class="text-gray-900"><%= Time.current.strftime('%Y-%m-%d %H:%M') %></span>
        </div>
      </div>
    </div>

    <!-- Notice -->
    <div class="bg-blue-50 rounded-lg p-4">
      <div class="flex gap-3">
        <svg class="w-5 h-5 text-blue-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
        <div class="text-sm text-blue-900">
          <p class="font-medium mb-1">温馨提示</p>
          <p>订单详情已发送至您的手机（<%= @cruise_order.contact_phone %>）。请保持手机畅通，等待商家联系确认。</p>
        </div>
      </div>
    </div>

    <!-- Actions -->
    <div class="space-y-3">
      <%= link_to "查看订单详情", cruise_order_path(@cruise_order), 
                  class: "block w-full text-center btn-primary py-3" %>
      <%= link_to "返回首页", root_path, 
                  class: "block w-full text-center px-6 py-3 border-2 border-gray-300 text-gray-700 font-medium rounded-lg hover:bg-gray-50 transition" %>
    </div>
  </div>
</div>
