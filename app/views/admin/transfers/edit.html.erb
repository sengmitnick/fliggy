<% content_for :title, "Edit Transfer" %>

<div class="container-md">
  <!-- Page Title -->
  <div class="flex items-center space-x-4 mb-6">
    <%= link_to admin_transfer_path(@transfer.id), class: "btn-ghost text-sm py-1 px-3" do %>
      <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      Back to Transfer
    <% end %>
    <h1 class="text-3xl font-bold text-gray-900 dark:text-gray-50">Edit Transfer</h1>
  </div>

  <!-- Form -->
  <div class="card bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-700">
    <div class="card-body">
      <%= form_with model: [:admin, @transfer], local: true, class: "space-y-6" do |form| %>
        <%= render 'shared/form_error_message', model: @transfer %>

        <!-- Transfer type -->
        <div class="mb-4">
          <%= form.label :transfer_type, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Transfer type<% if field_required?(@transfer, :transfer_type) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :transfer_type, {
            placeholder: "Enter transfer type...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :transfer_type) ? { required: true } : {}) %>
          <% if @transfer.errors[:transfer_type].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:transfer_type].first %></p>
          <% end %>
        </div>

        <!-- Service type -->
        <div class="mb-4">
          <%= form.label :service_type, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Service type<% if field_required?(@transfer, :service_type) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :service_type, {
            placeholder: "Enter service type...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :service_type) ? { required: true } : {}) %>
          <% if @transfer.errors[:service_type].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:service_type].first %></p>
          <% end %>
        </div>

        <!-- Location from -->
        <div class="mb-4">
          <%= form.label :location_from, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Location from<% if field_required?(@transfer, :location_from) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :location_from, {
            placeholder: "Enter location from...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :location_from) ? { required: true } : {}) %>
          <% if @transfer.errors[:location_from].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:location_from].first %></p>
          <% end %>
        </div>

        <!-- Location to -->
        <div class="mb-4">
          <%= form.label :location_to, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Location to<% if field_required?(@transfer, :location_to) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :location_to, {
            placeholder: "Enter location to...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :location_to) ? { required: true } : {}) %>
          <% if @transfer.errors[:location_to].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:location_to].first %></p>
          <% end %>
        </div>

        <!-- Pickup datetime -->
        <div class="mb-4">
          <%= form.label :pickup_datetime, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Pickup datetime<% if field_required?(@transfer, :pickup_datetime) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.datetime_local_field :pickup_datetime, {
            placeholder: "Enter pickup datetime...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :pickup_datetime) ? { required: true } : {}) %>
          <% if @transfer.errors[:pickup_datetime].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:pickup_datetime].first %></p>
          <% end %>
        </div>

        <!-- Flight number -->
        <div class="mb-4">
          <%= form.label :flight_number, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Flight number<% if field_required?(@transfer, :flight_number) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :flight_number, {
            placeholder: "Enter flight number...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :flight_number) ? { required: true } : {}) %>
          <% if @transfer.errors[:flight_number].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:flight_number].first %></p>
          <% end %>
        </div>

        <!-- Train number -->
        <div class="mb-4">
          <%= form.label :train_number, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Train number<% if field_required?(@transfer, :train_number) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :train_number, {
            placeholder: "Enter train number...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :train_number) ? { required: true } : {}) %>
          <% if @transfer.errors[:train_number].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:train_number].first %></p>
          <% end %>
        </div>

        <!-- Passenger name -->
        <div class="mb-4">
          <%= form.label :passenger_name, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Passenger name<% if field_required?(@transfer, :passenger_name) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :passenger_name, {
            placeholder: "Enter passenger name...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :passenger_name) ? { required: true } : {}) %>
          <% if @transfer.errors[:passenger_name].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:passenger_name].first %></p>
          <% end %>
        </div>

        <!-- Passenger phone -->
        <div class="mb-4">
          <%= form.label :passenger_phone, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Passenger phone<% if field_required?(@transfer, :passenger_phone) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :passenger_phone, {
            placeholder: "Enter passenger phone...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :passenger_phone) ? { required: true } : {}) %>
          <% if @transfer.errors[:passenger_phone].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:passenger_phone].first %></p>
          <% end %>
        </div>

        <!-- Vehicle type -->
        <div class="mb-4">
          <%= form.label :vehicle_type, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Vehicle type<% if field_required?(@transfer, :vehicle_type) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :vehicle_type, {
            placeholder: "Enter vehicle type...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :vehicle_type) ? { required: true } : {}) %>
          <% if @transfer.errors[:vehicle_type].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:vehicle_type].first %></p>
          <% end %>
        </div>

        <!-- Provider name -->
        <div class="mb-4">
          <%= form.label :provider_name, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Provider name<% if field_required?(@transfer, :provider_name) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :provider_name, {
            placeholder: "Enter provider name...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :provider_name) ? { required: true } : {}) %>
          <% if @transfer.errors[:provider_name].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:provider_name].first %></p>
          <% end %>
        </div>

        <!-- License plate -->
        <div class="mb-4">
          <%= form.label :license_plate, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            License plate<% if field_required?(@transfer, :license_plate) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :license_plate, {
            placeholder: "Enter license plate...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :license_plate) ? { required: true } : {}) %>
          <% if @transfer.errors[:license_plate].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:license_plate].first %></p>
          <% end %>
        </div>

        <!-- Driver name -->
        <div class="mb-4">
          <%= form.label :driver_name, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Driver name<% if field_required?(@transfer, :driver_name) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.text_field :driver_name, {
            placeholder: "Enter driver name...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :driver_name) ? { required: true } : {}) %>
          <% if @transfer.errors[:driver_name].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:driver_name].first %></p>
          <% end %>
        </div>

        <!-- Total price -->
        <div class="mb-4">
          <%= form.label :total_price, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Total price<% if field_required?(@transfer, :total_price) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.number_field :total_price, {
            placeholder: "Enter total price...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :total_price) ? { required: true } : {}) %>
          <% if @transfer.errors[:total_price].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:total_price].first %></p>
          <% end %>
        </div>

        <!-- Discount amount -->
        <div class="mb-4">
          <%= form.label :discount_amount, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Discount amount<% if field_required?(@transfer, :discount_amount) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.number_field :discount_amount, {
            placeholder: "Enter discount amount...",
            class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50"
          }.merge(field_required?(@transfer, :discount_amount) ? { required: true } : {}) %>
          <% if @transfer.errors[:discount_amount].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:discount_amount].first %></p>
          <% end %>
        </div>

        <!-- Status -->
        <div class="mb-4">
          <%= form.label :status, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Status<% if field_required?(@transfer, :status) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.select :status, 
              options_for_select([['Select Status', '']] + [["Pending", "pending"], ["Paid", "paid"], ["Cancelled", "cancelled"], ["Completed", "completed"], ["Refunded", "refunded"]], @transfer.status),
              {},
              { class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50" } %>
          <% if @transfer.errors[:status].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:status].first %></p>
          <% end %>
        </div>

        <!-- Driver status -->
        <div class="mb-4">
          <%= form.label :driver_status, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Driver status<% if field_required?(@transfer, :driver_status) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.select :driver_status, 
              options_for_select([['Select Driver status', '']] + [["Pending", "pending"], ["Accepted", "accepted"], ["Driving", "driving"], ["Arrived", "arrived"], ["Completed", "completed"], ["Cancelled", "cancelled"]], @transfer.driver_status),
              {},
              { class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50" } %>
          <% if @transfer.errors[:driver_status].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:driver_status].first %></p>
          <% end %>
        </div>

        <!-- User -->
        <div class="mb-4">
          <%= form.label :user_id, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            User<% if field_required?(@transfer, :user) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.select :user_id, 
              options_for_select([['Select User', '']] + User.all.map { |item| [item.name, item.id] }, @transfer.user_id),
              {},
              { class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50" } %>
          <% if @transfer.errors[:user_id].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:user_id].first %></p>
          <% end %>
        </div>

        <!-- Transfer package -->
        <div class="mb-4">
          <%= form.label :transfer_package_id, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Transfer package<% if field_required?(@transfer, :transfer_package) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.select :transfer_package_id, 
              options_for_select([['Select Transfer package', '']] + TransferPackage.all.map { |item| [item.name, item.id] }, @transfer.transfer_package_id),
              {},
              { class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50" } %>
          <% if @transfer.errors[:transfer_package_id].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:transfer_package_id].first %></p>
          <% end %>
        </div>

        <!-- Flight -->
        <div class="mb-4">
          <%= form.label :flight_id, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Flight<% if field_required?(@transfer, :flight) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.select :flight_id, 
              options_for_select([['Select Flight', '']] + Flight.all.map { |item| [item.id, item.id] }, @transfer.flight_id),
              {},
              { class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50" } %>
          <% if @transfer.errors[:flight_id].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:flight_id].first %></p>
          <% end %>
        </div>

        <!-- Train -->
        <div class="mb-4">
          <%= form.label :train_id, class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" do %>
            Train<% if field_required?(@transfer, :train) %> <span class="text-red-500">*</span><% end %>
          <% end %>
          <%= form.select :train_id, 
              options_for_select([['Select Train', '']] + Train.all.map { |item| [item.id, item.id] }, @transfer.train_id),
              {},
              { class: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:text-gray-50" } %>
          <% if @transfer.errors[:train_id].any? %>
            <p class="text-sm text-red-500 mt-1"><%= @transfer.errors[:train_id].first %></p>
          <% end %>
        </div>

        <!-- Form Actions -->
        <div class="flex justify-end space-x-3">
          <%= link_to admin_transfer_path(@transfer.id), class: "btn-ghost" do %>
            Cancel
          <% end %>
          <%= form.submit "Update Transfer", class: "btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>