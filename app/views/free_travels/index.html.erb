<div class="min-h-screen bg-white" 
     data-controller="city-selector"
     data-city-selector-departure-city-value="<%= @destination %>"
     data-city-selector-destination-city-value=""
     data-city-selector-enable-multi-select-value="false"
     data-city-selector-modal-title-value="选择目的地">
  <!-- Reuse the transport header with blue background -->
  <%= render 'shared/transport_header', background_image: '活动.png', return_path: root_path %>

  <!-- Tabs Container with Gray Background -->
  <div class="relative mx-4 -mt-3 overflow-hidden z-50">
    <!-- Custom Tabs for Tour Groups Page -->
    <div class="bg-[#F2F3F7] rounded-t-[24px] pt-2 px-1">
      <nav class="flex items-end">
        <!-- Inactive Tab: 跟团游 -->
        <%= link_to tour_groups_path, class: "px-2 py-2 flex-1 text-[13px] font-medium text-[#666666] cursor-pointer whitespace-nowrap flex flex-col items-center" do %>
          跟团游
        <% end %>
        
        <!-- Inactive Tab: 独立成团 -->
        <%= link_to private_groups_path, class: "px-2 py-2 flex-1 text-[13px] font-medium text-[#666666] cursor-pointer whitespace-nowrap flex flex-col items-center" do %>
          独立成团
        <% end %>
        
        <!-- Active Tab: 自由出行 -->
        <div class="relative bg-white flex-1 rounded-t-[12px] px-3 py-2.5 cursor-pointer">
          <div class="flex flex-col items-center">
            <span class="text-[15px] font-bold text-[#1a1a1a] leading-tight whitespace-nowrap">自由出行</span>
            <div class="absolute bottom-1 w-6 h-[4px] bg-[#FFD900] rounded-full"></div>
          </div>
        </div>
      </nav>
    </div>

    <!-- White Card Container with Form -->
    <div class="bg-white rounded-b-[24px] shadow-sm px-4 pt-4 pb-6" 
         data-controller="tour-group-filter"
         data-tour-group-filter-destination-value="<%= @destination %>"
         data-tour-group-filter-duration-value=""
         data-tour-group-filter-group-size-value="<%= @group_size %>">
      
      <!-- Destination Section -->
      <div class="mb-4">
        <button 
          type="button"
          data-action="click->city-selector#openDeparture"
          class="w-full text-left flex items-center justify-between">
          <div>
            <div class="text-sm text-gray-600 mb-1">我想去</div>
            <div class="text-[32px] font-bold leading-tight" 
                 data-city-selector-target="departure"
                 data-tour-group-filter-target="departureDisplay"><%= @destination %></div>
          </div>
          <svg class="w-6 h-6 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
          </svg>
        </button>
      </div>

      <!-- Hot Destinations -->
      <div class="mb-4">
        <div class="flex flex-wrap gap-2">
          <% @hot_destinations.each do |city| %>
            <button 
              type="button"
              data-tour-group-filter-target="destinationButton"
              data-destination="<%= city %>"
              data-action="click->tour-group-filter#selectDestination"
              class="<%= city == @destination ? 'bg-[#FFE8CC] border-2 border-[#FFD700] shadow-md' : 'bg-gray-50 border-2 border-transparent' %> px-4 py-2 rounded-lg flex items-center gap-1 text-sm transition-all">
              <span class="text-base">🔥</span>
              <span class="<%= city == @destination ? 'font-bold' : 'font-medium' %>"><%= city %></span>
            </button>
          <% end %>
        </div>
      </div>

      <!-- Duration Selection -->
      <div class="mb-4">
        <div class="flex items-center justify-between mb-2">
          <div class="text-sm text-gray-600" data-tour-group-filter-target="durationLabel">
            <% if @duration.present? && @duration.to_i > 3 %>
              玩<%= @duration %>天
            <% else %>
              天数
            <% end %>
          </div>
          <div class="flex items-center gap-2">
            <!-- Clear button (X icon) - only show when duration is selected -->
            <button 
              type="button"
              data-action="click->tour-group-filter#clearDuration"
              data-tour-group-filter-target="clearDurationButton"
              class="<%= @duration.present? ? '' : 'hidden' %> text-gray-400 hover:text-gray-600 transition-colors">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
              </svg>
            </button>
            <!-- Arrow button for opening modal -->
            <button 
              type="button"
              data-action="click->tour-group-filter#openDurationModal"
              class="text-gray-500 hover:text-gray-700 transition-colors">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
              </svg>
            </button>
          </div>
        </div>
        <!-- Quick duration buttons - shown when no extended duration selected -->
        <div class="<%= @duration.present? && @duration.to_i > 3 ? 'hidden' : 'flex gap-3' %>" data-tour-group-filter-target="quickDurationButtons">
          <% @duration_quick_options.each do |option| %>
            <button 
              type="button"
              data-tour-group-filter-target="durationButton"
              data-duration="<%= option[:value] %>"
              data-action="click->tour-group-filter#selectDuration"
              class="<%= option[:value] == @duration ? 'bg-[#FFE8CC] border-2 border-[#FFD700] shadow-md' : 'bg-gray-50 border-2 border-transparent' %> flex-1 py-3 rounded-lg text-sm relative transition-all">
              <span class="<%= option[:value] == @duration ? 'font-bold' : 'font-medium' %>"><%= option[:label] %></span>
              <% if option[:hot] %>
                <span class="absolute -top-1 -right-1 text-base">🔥</span>
              <% end %>
            </button>
          <% end %>
        </div>
      </div>

      <!-- Group Size Selection -->
      <div class="mb-4">
        
      </div>

      <!-- Search Button Area with Customization Button -->
      <div class="flex gap-3">
        <!-- 为我定制按钮 (1/3 width) -->
        <button type="button" class="flex-1 py-4 rounded-full text-lg font-bold text-black" style="background: #FFD700;">
          为我定制
        </button>
        
        <!-- 搜索按钮 (2/3 width) -->
        <%= form_with url: search_tour_groups_path, method: :get, local: true, class: 'flex-[2]', data: { turbo: false } do |f| %>
          <%= f.hidden_field :destination, value: @destination, data: { tour_group_filter_target: 'destinationInput', city_selector_target: 'departureCityInput' } %>
          <%= f.hidden_field :duration, data: { tour_group_filter_target: 'durationInput' } %>
          <%= f.hidden_field :group_size, data: { tour_group_filter_target: 'groupSizeInput' } %>
          <%= f.hidden_field :travel_type, value: '自由出行' %>
          <!-- Hidden destination target (required by city-selector controller but not used) -->
          <span class="hidden" data-city-selector-target="destination"></span>
          <%= f.hidden_field :unused_destination, value: '', data: { city_selector_target: 'destinationCityInput' }, class: 'hidden' %>
          <button type="submit" class="w-full py-4 rounded-full text-lg font-bold text-black" style="background: #FFD700;">
            搜索自由出行商品
          </button>
        <% end %>
      </div>

      <!-- Duration Selection Modal (inside controller scope) -->
      <div 
        data-tour-group-filter-target="durationModal"
        class="fixed inset-0 bg-black/50 z-50 hidden"
        data-action="click->tour-group-filter#closeDurationModalOnBackdrop">
        <div class="absolute bottom-0 left-0 right-0 bg-white rounded-t-3xl max-h-[80vh] overflow-y-auto shadow-2xl">
          <!-- Modal Header -->
          <div class="sticky top-0 bg-white px-6 py-5 flex items-center justify-center relative border-b border-gray-100">
            <h3 class="text-lg font-semibold text-gray-900">选择天数</h3>
            <button 
              type="button"
              data-action="click->tour-group-filter#cancelDurationModal"
              class="absolute right-4 w-8 h-8 flex items-center justify-center rounded-full hover:bg-gray-100 text-gray-400 hover:text-gray-600 transition-colors">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
              </svg>
            </button>
          </div>

          <!-- Duration Options Grid -->
          <div class="px-4 py-5">
            <div class="grid grid-cols-4 gap-2 mb-6">
              <% @duration_options.each do |option| %>
                <button 
                  type="button"
                  data-action="click->tour-group-filter#selectDurationInModal"
                  data-duration="<%= option[:value] %>"
                  data-tour-group-filter-target="durationOption"
                  class="px-3 py-3 rounded-lg text-sm font-medium bg-gray-100 text-gray-700 hover:bg-gray-200 transition-colors">
                  <%= option[:label] %>
                </button>
              <% end %>
            </div>
            
            <!-- Bottom Action Buttons -->
            <div class="flex gap-3">
              <button 
                type="button"
                data-action="click->tour-group-filter#clearDurationInModal"
                class="flex-1 py-3.5 rounded-full bg-gray-100 text-gray-700 text-base font-medium hover:bg-gray-200 transition-colors">
                清空
              </button>
              <button 
                type="button"
                data-action="click->tour-group-filter#confirmDurationSelection"
                class="flex-[2] py-3.5 rounded-full text-base font-medium transition-colors"
                style="background: linear-gradient(135deg, #FFD700 0%, #FFC700 100%); color: #000;">
                确认
              </button>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>

  <!-- Tour Recommendations Section -->
  <div class="mt-6 px-4">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-xl font-bold">自由出行精选推荐</h2>
      <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
      </svg>
    </div>

    <!-- Seasonal Recommendations -->
    <div class="mb-6">
      <h3 class="text-base font-bold mb-3">当季推荐 <span class="text-xs font-normal text-gray-400 ml-1">捕捉季节灵感，享品质旅行</span></h3>
      
      <!-- Recommendation Tabs -->
      <div class="flex gap-2 mb-4 overflow-x-auto hide-scrollbar pb-1">
        <button class="px-4 py-2 rounded-full text-sm font-bold whitespace-nowrap" style="background: #FFD700;">
          春节爆款
        </button>
        <button class="px-4 py-2 rounded-full bg-gray-100 text-sm font-medium whitespace-nowrap">
          近期热销
        </button>
        <button class="px-4 py-2 rounded-full bg-gray-100 text-sm font-medium whitespace-nowrap">
          当季新品
        </button>
        <button class="px-4 py-2 rounded-full bg-gray-100 text-sm font-medium whitespace-nowrap">
          超值特惠
        </button>
      </div>

      <!-- Tour Cards -->
      <div class="flex gap-3 overflow-x-auto hide-scrollbar">
        <% @tour_recommendations.each do |tour| %>
          <%= link_to tour_group_path(tour[:id]), class: "min-w-[260px] bg-white rounded-2xl overflow-hidden shadow-sm border border-gray-100 hover:shadow-md transition-shadow" do %>
            <!-- Tour Image -->
            <div class="relative h-40 bg-gray-200">
              <%= image_tag tour[:image], class: 'w-full h-full object-cover', alt: tour[:title] %>
              <span class="absolute top-2 left-2 bg-black/60 text-white text-xs px-2 py-1 rounded">
                <%= tour[:badge] %>
              </span>
            </div>
            
            <!-- Tour Info -->
            <div class="p-3">
              <h4 class="text-sm font-medium leading-tight mb-2 line-clamp-2 min-h-[40px]">
                <%= tour[:title] %>
              </h4>
              <div class="flex items-center justify-between">
                <span class="text-red-500 font-bold text-base"><%= tour[:price] %></span>
                <% if tour[:sales].present? %>
                  <span class="text-xs text-gray-400"><%= tour[:sales] %></span>
                <% end %>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>

    <!-- Domestic Tours Section -->
    <div class="mb-6">
      <div class="flex items-center justify-between mb-4">
        <h3 class="text-xl font-bold">国内游</h3>
        <button class="text-sm text-gray-500 flex items-center gap-1">
          更多
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
          </svg>
        </button>
      </div>
      
      <!-- Destination Cards -->
      <div class="grid grid-cols-2 gap-3">
        <!-- Beijing Card -->
        <div class="bg-gray-900 rounded-2xl overflow-hidden h-40 relative">
          <%= image_tag 'https://images.unsplash.com/photo-1508804185872-d7badad00f7d?w=400', class: 'absolute inset-0 w-full h-full object-cover opacity-60', alt: '北京' %>
          <div class="absolute inset-0 p-4 flex flex-col justify-between">
            <h4 class="text-white text-xl font-bold">北京</h4>
            <p class="text-white text-xs opacity-90">北京环球度假...</p>
          </div>
        </div>
        
        <!-- Shanghai Card -->
        <div class="bg-blue-900 rounded-2xl overflow-hidden h-40 relative">
          <%= image_tag 'https://images.unsplash.com/photo-1537906750095-5f260e1d4922?w=400', class: 'absolute inset-0 w-full h-full object-cover opacity-60', alt: '上海' %>
          <div class="absolute inset-0 p-4 flex flex-col justify-between">
            <h4 class="text-white text-xl font-bold">上海</h4>
            <p class="text-white text-xs opacity-90">上海迪士尼...</p>
          </div>
        </div>
      </div>
    </div>

    <!-- International Tours Section -->
    <div class="mb-20">
      <div class="flex items-center justify-between mb-4">
        <h3 class="text-xl font-bold">出境游</h3>
        <button class="text-sm text-gray-500 flex items-center gap-1">
          更多
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/>
          </svg>
        </button>
      </div>
      
      <!-- Destination Cards -->
      <div class="grid grid-cols-2 gap-3">
        <!-- Tokyo Card -->
        <div class="bg-red-900 rounded-2xl overflow-hidden h-40 relative">
          <%= image_tag 'https://images.unsplash.com/photo-1540959733332-eab4deabeeaf?w=400', class: 'absolute inset-0 w-full h-full object-cover opacity-60', alt: '东京' %>
          <div class="absolute inset-0 p-4 flex flex-col justify-between">
            <h4 class="text-white text-xl font-bold">东京</h4>
            <p class="text-white text-xs opacity-90">涩谷天空天...</p>
          </div>
        </div>
        
        <!-- Singapore Card -->
        <div class="bg-green-900 rounded-2xl overflow-hidden h-40 relative">
          <%= image_tag 'https://images.unsplash.com/photo-1525625293386-3f8f99389edd?w=400', class: 'absolute inset-0 w-full h-full object-cover opacity-60', alt: '新加坡' %>
          <div class="absolute inset-0 p-4 flex flex-col justify-between">
            <h4 class="text-white text-xl font-bold">新加坡</h4>
            <p class="text-white text-xs opacity-90">新加坡环球...</p>
          </div>
        </div>
      </div>
    </div>

  </div>

  <!-- City Selector Modal -->
  <%= render 'shared/city_selector_modal' %>

  <!-- Bottom Navigation -->
  <%= render 'shared/bottom_navigation' %>
</div>

<style>
  .hide-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .hide-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
