<div class="min-h-screen bg-gray-100 pb-24">
  <!-- Header -->
  <div class="bg-white border-b sticky top-0 z-10">
    <div class="container mx-auto px-4 py-4 flex items-center">
      <%= link_to profile_path, class: "text-2xl mr-4" do %>
        ←
      <% end %>
      <h1 class="text-lg font-bold flex-1"><%= @user.has_pay_password? ? '修改支付密码' : '设置支付密码' %></h1>
    </div>
  </div>

  <!-- Form -->
  <div class="container mx-auto px-4 py-6">
    <div class="bg-white rounded-lg shadow">
      <%= form_with model: @user, url: update_pay_password_profile_path, method: :patch, local: true do |form| %>
        <%= render 'shared/form_error_message', model: @user %>

        <% if @user.has_pay_password? %>
          <!-- Current Pay Password -->
          <div class="p-4 border-b">
            <%= form.label :current_pay_password, class: "block text-sm text-gray-600 mb-2" do %>
              当前支付密码
            <% end %>
            <%= form.password_field :current_pay_password, 
                                    required: true,
                                    maxlength: 6,
                                    pattern: "[0-9]{6}",
                                    placeholder: "请输入6位数字密码",
                                    class: "w-full px-4 py-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" %>
            <p class="text-xs text-gray-400 mt-1">请输入当前的6位数字支付密码</p>
          </div>
        <% end %>

        <!-- New Pay Password -->
        <div class="p-4 border-b">
          <%= form.label :pay_password, class: "block text-sm text-gray-600 mb-2" do %>
            <%= @user.has_pay_password? ? '新支付密码' : '支付密码' %>
          <% end %>
          <%= form.password_field :pay_password, 
                                  required: true,
                                  maxlength: 6,
                                  pattern: "[0-9]{6}",
                                  placeholder: "请输入6位数字密码",
                                  class: "w-full px-4 py-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" %>
          <p class="text-xs text-gray-400 mt-1">支付密码用于支付确认，请设置6位数字密码</p>
        </div>

        <!-- Confirm Pay Password -->
        <div class="p-4 border-b">
          <%= form.label :pay_password_confirmation, class: "block text-sm text-gray-600 mb-2" do %>
            确认支付密码
          <% end %>
          <%= form.password_field :pay_password_confirmation, 
                                  required: true,
                                  maxlength: 6,
                                  pattern: "[0-9]{6}",
                                  placeholder: "请再次输入6位数字密码",
                                  class: "w-full px-4 py-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" %>
        </div>

        <!-- Submit Button -->
        <div class="p-4">
          <%= form.submit @user.has_pay_password? ? '修改密码' : '设置密码', 
                          class: "w-full bg-blue-500 text-white py-3 rounded-lg text-lg font-bold hover:bg-blue-600 transition" %>
        </div>
      <% end %>
    </div>

    <!-- Security Tips -->
    <div class="mt-4 bg-white rounded-lg shadow p-4">
      <h3 class="font-bold mb-2 text-sm">安全提示：</h3>
      <ul class="text-xs text-gray-600 space-y-1">
        <li>• 支付密码仅用于支付确认，请勿设置为与登录密码相同</li>
        <li>• 请设置6位数字密码，建议不要使用过于简单的密码（如123456）</li>
        <li>• 请勿将支付密码告知他人</li>
        <li>• 定期修改支付密码可以提高账户安全性</li>
      </ul>
    </div>
  </div>
</div>
