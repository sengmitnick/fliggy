<div class="bg-surface min-h-screen flex flex-col items-center justify-center px-4">
  <!-- 成功图标 -->
  <div class="w-24 h-24 rounded-full bg-green-100 flex items-center justify-center mb-6 animate-bounce">
    <svg class="w-12 h-12 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
  </div>

  <h1 class="text-2xl font-bold text-gray-900 mb-2">支付成功！</h1>
  <p class="text-sm text-gray-600 text-center mb-8">保单将在24小时内生效<br/>电子保单将发送至您的邮箱</p>

  <!-- 订单信息卡片 -->
  <div class="w-full max-w-md bg-white rounded-2xl p-6 shadow-lg mb-8">
    <div class="text-center mb-6">
      <div class="text-sm text-gray-500 mb-2">订单号</div>
      <div class="text-base font-mono text-gray-900"><%= @order.order_number %></div>
    </div>

    <div class="flex items-center justify-center mb-6">
      <% if @order.insurance_product.image_url.present? %>
        <div class="w-16 h-16 rounded-xl overflow-hidden bg-gray-100 mr-3">
          <img src="<%= @order.insurance_product.image_url %>" alt="<%= @order.insurance_product.name %>" class="w-full h-full object-cover">
        </div>
      <% else %>
        <div class="w-16 h-16 rounded-xl bg-gradient-to-br from-blue-100 to-purple-100 flex items-center justify-center mr-3">
          <svg class="w-8 h-8 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>
        </div>
      <% end %>

      <div class="flex-1">
        <div class="font-bold text-sm text-gray-900 line-clamp-1"><%= @order.insurance_product.name %></div>
        <div class="text-xs text-gray-500 mt-1"><%= @order.insurance_product.company %></div>
      </div>
    </div>

    <div class="bg-blue-50 rounded-xl p-4 border border-blue-100 mb-4">
      <div class="text-xs text-gray-600 text-center mb-1">保障期限</div>
      <div class="text-sm font-bold text-gray-900 text-center">
        <%= @order.start_date.strftime('%Y-%m-%d') %> 至 <%= @order.end_date.strftime('%Y-%m-%d') %>
      </div>
      <div class="text-xs text-gray-500 text-center mt-1">共<%= @order.days %>天</div>
    </div>

    <div class="flex items-baseline justify-center pt-4 border-t border-gray-100">
      <span class="text-sm text-gray-500 mr-1">保费总计</span>
      <span class="text-xs text-gray-500">¥</span>
      <span class="text-2xl font-bold text-orange-600"><%= @order.total_price.to_i %></span>
    </div>
  </div>

  <!-- 操作按钮 -->
  <div class="w-full max-w-md space-y-3">
    <%= link_to insurance_order_path(@order), class: "block w-full btn-primary py-3 text-center font-bold rounded-full shadow-lg" do %>
      查看订单详情
    <% end %>

    <%= link_to my_orders_insurance_orders_path, class: "block w-full btn-outline py-3 text-center font-medium rounded-full" do %>
      我的保险订单
    <% end %>

    <%= link_to insurances_path, class: "block w-full text-center text-sm text-gray-500 py-2" do %>
      继续投保其他产品
    <% end %>
  </div>

  <!-- 提示信息 -->
  <div class="w-full max-w-md mt-8 bg-blue-50 rounded-xl p-4 border border-blue-100">
    <div class="flex items-start gap-3">
      <svg class="w-5 h-5 text-blue-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/></svg>
      <div class="flex-1 text-xs text-blue-700 leading-relaxed">
        <div class="font-bold mb-1">温馨提示</div>
        <div class="space-y-1">
          <div>• 电子保单将在24小时内发送至您的邮箱</div>
          <div>• 保单生效前出险无法理赔，请注意保障起始时间</div>
          <div>• 如需理赔请联系保险公司客服</div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
@keyframes bounce {
  0%, 100% {
    transform: translateY(-5%);
    animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
  }
  50% {
    transform: translateY(0);
    animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
  }
}

.animate-bounce {
  animation: bounce 1s infinite;
}
</style>
