require 'rails_helper'

RSpec.describe VehicleType, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:vehicle_type)
  #   expect(record).to be_persisted
  # end
end
