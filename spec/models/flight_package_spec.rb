require 'rails_helper'

RSpec.describe FlightPackage, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:flight_package)
  #   expect(record).to be_persisted
  # end
end
