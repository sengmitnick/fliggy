require 'rails_helper'

RSpec.describe CruiseOrder, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:cruise_order)
  #   expect(record).to be_persisted
  # end
end
