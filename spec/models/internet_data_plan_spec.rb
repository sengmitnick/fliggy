require 'rails_helper'

RSpec.describe InternetDataPlan, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:internet_data_plan)
  #   expect(record).to be_persisted
  # end
end
