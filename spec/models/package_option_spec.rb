require 'rails_helper'

RSpec.describe PackageOption, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:package_option)
  #   expect(record).to be_persisted
  # end
end
