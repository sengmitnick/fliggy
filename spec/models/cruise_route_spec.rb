require 'rails_helper'

RSpec.describe CruiseRoute, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:cruise_route)
  #   expect(record).to be_persisted
  # end
end
