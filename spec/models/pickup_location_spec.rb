require 'rails_helper'

RSpec.describe PickupLocation, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:pickup_location)
  #   expect(record).to be_persisted
  # end
end
