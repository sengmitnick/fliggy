require 'rails_helper'

RSpec.describe TourPackage, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:tour_package)
  #   expect(record).to be_persisted
  # end
end
