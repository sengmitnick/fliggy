require 'rails_helper'

RSpec.describe CruiseSailing, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:cruise_sailing)
  #   expect(record).to be_persisted
  # end
end
