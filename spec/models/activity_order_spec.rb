require 'rails_helper'

RSpec.describe ActivityOrder, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:activity_order)
  #   expect(record).to be_persisted
  # end
end
