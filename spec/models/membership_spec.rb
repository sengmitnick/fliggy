require 'rails_helper'

RSpec.describe Membership, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:membership)
  #   expect(record).to be_persisted
  # end
end
