require 'rails_helper'

RSpec.describe InternetSimCard, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:internet_sim_card)
  #   expect(record).to be_persisted
  # end
end
