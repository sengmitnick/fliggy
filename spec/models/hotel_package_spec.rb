require 'rails_helper'

RSpec.describe HotelPackage, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:hotel_package)
  #   expect(record).to be_persisted
  # end
end
