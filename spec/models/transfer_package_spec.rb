require 'rails_helper'

RSpec.describe TransferPackage, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:transfer_package)
  #   expect(record).to be_persisted
  # end
end
