require 'rails_helper'

RSpec.describe HotelPolicy, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:hotel_policy)
  #   expect(record).to be_persisted
  # end
end
