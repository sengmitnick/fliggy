require 'rails_helper'

RSpec.describe CruiseShip, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:cruise_ship)
  #   expect(record).to be_persisted
  # end
end
