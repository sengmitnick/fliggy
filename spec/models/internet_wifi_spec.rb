require 'rails_helper'

RSpec.describe InternetWifi, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:internet_wifi)
  #   expect(record).to be_persisted
  # end
end
